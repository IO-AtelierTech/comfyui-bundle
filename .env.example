# =============================================================================
# ComfyUI Setup - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# fal.ai Cloud GPU (Required for remote inference)
# -----------------------------------------------------------------------------
# Get your API key at: https://fal.ai/dashboard/keys
FAL_KEY=your-fal-api-key-here

# -----------------------------------------------------------------------------
# ComfyUI Server Settings
# -----------------------------------------------------------------------------
# URL where ComfyUI is running (default: http://localhost:8188)
COMFY_URL=http://localhost:8188

# External URL for image retrieval (if different from COMFY_URL)
# Useful when ComfyUI runs in Docker with different internal/external ports
COMFY_URL_EXTERNAL=http://localhost:8188

# -----------------------------------------------------------------------------
# MCP Server Settings (for Claude Code integration)
# -----------------------------------------------------------------------------
# Directory containing workflow JSON files
# Change this to your actual workflows directory path
COMFY_WORKFLOWS_DIR=/path/to/comfyui-setup/workflows

# Default workflow file for generate_image() tool
# Must be an API-format JSON exported from ComfyUI
COMFY_WORKFLOW_JSON_FILE=/path/to/comfyui-setup/workflows/default.json

# Node IDs for the default workflow
# These correspond to nodes in your default.json workflow
PROMPT_NODE_ID=6
OUTPUT_NODE_ID=9

# Output mode: "file" returns image data, "url" returns image URL
OUTPUT_MODE=file

# Polling settings for workflow execution
# POLL_TIMEOUT: Max seconds to wait for workflow completion (1-300)
# POLL_INTERVAL: Seconds between status checks (0.1-10.0)
POLL_TIMEOUT=120
POLL_INTERVAL=2.0
